Botón gris con bisel Win9x; `pressed` lo hunde (tabs de taskbar, toggles).

```jsx
<BevelButton bold>Aplicar</BevelButton>
<BevelButton pressed>mastodon</BevelButton>
<StartButton />
```

Nunca usar radios ni sombras en botones — el estado se comunica solo con el bisel.
