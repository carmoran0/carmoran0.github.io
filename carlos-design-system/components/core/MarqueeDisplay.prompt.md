Display LED negro con marquee verde fósforo (reproductor last.fm).

```jsx
<MarqueeDisplay text="♪ ARTISTA — CANCIÓN SONANDO AHORA ♪" subtext="84.201 scrobbles" />
```

Requiere `styles.css` cargado (define `@keyframes marquee`).
